Go through the following list of issues and fixes:

1. changelog/v1.0/1.png is the screenshot from the live app. Move the navigation bar just above the bottom of the screen it way above.
2. watch carefully you will see in changelog/v1.0/1.png that navigation bar icons and background sizing of the pills is not accurate. see the screenshot from the template changelog/v1.0/2.png
3. Also changelog/v1.0/1.png - the orange pill in the navigation bar should move smoothly between the navigation options. like if i should be able to scroll from one option to another.
4. go through the changelog/v1.0/3.png ( template screenshot) vs the changelog/v1.0/4.png. the menu sizing is wrong.
5. If i add a program to my programs and set it as a default - it should implement from the day it was made default. It shouldnt show or replace/delete the previous workouts that had been done before. if nothing was scheduled then they should remain empty. only from the day that a program became default it should start scheduling for the future.
6. More over add a option once you set a program as default - you should be able to choice from when it should implement and schedule it - today, tomorrow or use a date on map. till that specific date keep the old program as scheduled or if empty then leave it as a rest day.
7. Also if a program has been scheduled to have a rest day then I am not able to see the rest day screen where i was able to add a workout or log a past workout.
8. Fix each menu's sizing all are either very long make them exactly the same as the template.