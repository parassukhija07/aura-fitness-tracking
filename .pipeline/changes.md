# IMPLEMENTATION SUMMARY

## 🔄 WHAT CHANGED
Wired the real DB-backed Plan views (`MyPlansView`, `ProgramLibraryView`, `WorkoutLibraryView`, `ExerciseLibraryTabView`) into `PlanTabView`'s four subtabs, added the missing `SaveEditScopeSheet` type required by `WorkoutEditorView`, and registered all 9 previously-orphaned Plan files plus the new `SaveEditScopeSheet.swift` in the Xcode build target (audit finding N2).

## 📁 MODIFIED FILES
- `AuraFitness/Plan/PlanTabView.swift` (full-file rewrite, 59 lines): kept `import SwiftUI`, `struct PlanTabView`, `appState`, `Subtab` enum, `subtab` state, and `navbar`; deleted the `schedule`/`workouts`/`modal`/`editingWk`/`viewingProg`/`editingProg`/`viewingEx` `@State` props and `calStartSun`; deleted the `shell`, `myPlansBody`, `assignDay`/`makeRest`/`deleteWorkout`, and `modalView(_:)` dead machinery; replaced `body` with the exact spec-given switch over `subtab` mounting `MyPlansView()` / `ProgramLibraryView()` bare and `WorkoutLibraryView()` / `ExerciseLibraryTabView()` each wrapped in their own `NavigationStack`.
- `AuraFitness.xcodeproj/project.pbxproj` (+40 lines, 0 deletions, additive-only diff confirmed via `git diff --stat`): inserted 10 `PBXBuildFile` entries after line 64 (original `PlanBodyMap.swift in Sources` line), 10 `PBXFileReference` entries after line 139 (original `PlanBodyMap.swift` file-ref line), 10 children entries into the `Plan` PBXGroup (`ACGRP060000000000000000A`) after its `PlanBodyMap.swift` child, and 10 entries into the Sources build phase `files` list after its `PlanBodyMap.swift in Sources` entry — for the 9 orphan files (`MyPlansView.swift`, `ProgramLibraryView.swift`, `ProgramDetailView.swift`, `ProgramEditorView.swift`, `WorkoutLibraryView.swift`, `WorkoutEditorView.swift`, `ExerciseLibraryView.swift`, `ExerciseDetailView.swift`, `CreateExerciseView.swift`) plus `SaveEditScopeSheet.swift`, using the exact `CA01.../CB01...` UUID table given in the spec.

## 🆕 NEW FILES
- `AuraFitness/Plan/SaveEditScopeSheet.swift` (38 lines): defines `struct SaveEditScopeSheet: View` with `onJustToday: (() -> Void)?` and `onPermanently: @escaping () -> Void`, satisfying the exact call site at `WorkoutEditorView.swift:96-103`; conditionally renders an `AuraTintedButton("Just for Today")` only when `onJustToday != nil`, always renders `AuraPrimaryButton("Save Permanently", icon: "checkmark")` and `AuraGrayButton("Cancel")`, each dismissing via `@Environment(\.dismiss)` after firing its closure.

## ⚠️ DEVIATION FROM SPEC (coordinator-approved)
The spec's line 76 said to keep `navbar` "unchanged" verbatim, but line 77 mandated deleting the `modal` `@State` property — and the original `navbar`'s `+` button action was `PlanIconButton(icon: "plus", ...) { modal = .addPlan }`, which cannot compile once `modal` is gone. This was a genuine contradiction in the spec (also evidenced by line 101's "VERIFY" allowed-symbol list, which already omits `modal`/`PlanModal`). I halted and surfaced this to the coordinator rather than guessing.

**Resolution (per coordinator instruction):** removed the `PlanIconButton(icon: "plus", size: 20, accent: true) { modal = .addPlan }` line and the now-purposeless `Spacer()` companion was **kept** (not removed) to preserve the `HStack`'s left-aligned title layout with minimal structural change. Title text, styling, filter-chip `ScrollView`, and all padding/tokens in `navbar` remain byte-identical to the original. Rationale (coordinator's): each DB-backed subtab view already owns its own create affordance (`MyPlansView`/`ProgramLibraryView` are self-contained with their own add flows; `WorkoutLibraryView`/`ExerciseLibraryTabView` have their own toolbar `+` inside their `NavigationStack` wrapper), so the navbar's global `+` was redundant once mock routing was removed.

## ✅ PBXPROJ VALIDATION CHECKLIST — ALL PASSED
- **10 new lines in each of the 4 sections (40 total):** confirmed — `git diff --stat` shows `1 file changed, 40 insertions(+)`, `0 deletions`.
- **Every `CA0100...` id appears exactly 3×, every `CB0100...` id appears exactly 2×:** confirmed via grep — 10 unique `CA01...` ids × 3 occurrences = 30 total matches; 10 unique `CB01...` ids × 2 occurrences = 20 total matches. Verified per-id via `-o` listing: each `CA01...000A00NN` appears once in the file-reference section (~line 150-159), once as a group child (~line 300-309), and once as a `fileRef=` inside its PBXBuildFile twin (~line 65-74). Each `CB01...000A00NN` appears once as a PBXBuildFile def (~line 65-74) and once in the Sources phase list (~line 507-516).
- **No pre-existing UUID collision:** confirmed — extracted the original file from `git show HEAD:...pbxproj` and grepped for the `CA01000000000000000A00\d\d` / `CB01000000000000000A00\d\d` patterns against it; 0 matches, i.e. none of the 20 new UUIDs existed in the file before this change.
- **Comment text matches real filenames, not struct names:** confirmed by inspection — e.g. `CA01000000000000000A0007 /* ExerciseLibraryView.swift */` (not `ExerciseLibraryTabView`), `CA01000000000000000A0008 /* ExerciseDetailView.swift */` (not `ExerciseEntryDetailView`).
- **Braces/commas balanced, closing `);` intact:** confirmed — whole-file brace count 200 open / 200 close, paren count 37 open / 37 close; visually confirmed the `Plan` group's closing `);`/`path = Plan;` and the Sources phase's continuing list are both intact immediately after the inserted blocks.

## 🎯 TESTER FOCUS AREAS
- **`PlanTabView` subtab switch:** verify each of the 4 filter chips (`My Plans`, `Programs`, `Workouts`, `Exercises`) mounts the correct real view and that `WorkoutLibraryView`/`ExerciseLibraryTabView` (wrapped in their own `NavigationStack`) still get working toolbar `+` buttons and push navigation, while `MyPlansView`/`ProgramLibraryView` (mounted bare) don't show a double nav bar.
- **`SaveEditScopeSheet` conditional rendering:** verify that when `onJustToday == nil` (the only current caller, `WorkoutEditorView.swift:96-103`) the "Just for Today" button is absent, and that both "Save Permanently" and "Cancel" always dismiss the sheet correctly.
- **Navbar `+` button removal:** verify the Plan tab's top navbar no longer shows a global "+" button (this is an intentional, coordinator-approved behavior change from the mock version) and that the title row still renders correctly left-aligned with the filter chips below it.
- **pbxproj integrity:** if a local/CI Xcode toolchain becomes available, do a clean build to confirm all 10 newly-registered files compile and link — this pass was static-only (no local toolchain).
